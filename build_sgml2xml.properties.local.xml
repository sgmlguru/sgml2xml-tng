<?xml version="1.0" encoding="UTF-8"?>
<properties>
    
    <!-- Set up current run and module -->
    <debug value="true"/>
    <verbose value="false"/>
    <wellformed-only value="false"/>
    <current-module value="ata"/>
    
    
    <!-- Change accordingly for repository path -->
    <repos>
        <dir location="/home/ari/Documents/repos"/>
        <sgm2xml location="${repos.dir}/sgml2xml-tng"/>
    </repos>
    
    
    <!-- Project environment; ${env.src} is a folder path
         inside your main project as given by ${base.dir} -->
    <env>
        <src value="doc"/>
    </env>
    
    <!-- ${base.dir} should be the conversion's base location, 
         minus the ${env.src}/data part set by the conversion. -->
    <base>
        <dir location="/home/ari/Documents/projects/tng-sgml2xml"/>
    </base>
    
</properties>
<?xml version="1.0" encoding="UTF-8"?>
<properties>
    
    <module location="${modules}/doc">
        <schemas location="${module}/schemas">
            <!-- SGML -->
            <input location="${module.schemas}/sgml/catalog.txt"/>
            <!-- SGML to XML conversion -->
            <output location="${module.schemas}/sgml/catalog_sgml2xml.txt"/>
            <!-- ATA resources -->
            <resources>
                <ent location="${module.schemas}/sgml/ISOent-declarations-xml.txt"/>
            </resources>
            <!-- SGML DOCTYPE lookup -->
            <sgml-doctypes location="${module.schemas}/sgml/doctype-lookup.xml"/>
            <!-- Inclusion elements -->
            <inclusions>
                <!-- EMPTY elements, whitespace-separated -->
                <empty value=""/>
            </inclusions>
        </schemas>
        
        <!-- Schematron -->
        <sch location="${module}/sch/test.sch"/>
        
        <!-- XProc pipeline manifests and libs -->
        <pipeline>
            <manifest location="${module}/pipelines/doc-xslt-manifest.xml"/>
            <xproc location="${module}/xproc/doc-migration.xpl"/>
        </pipeline>
    </module>
    
</properties>
